<Window x:Class="Demo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Demo"
        mc:Ignorable="d"
        xmlns:uc="clr-namespace:Demo.UserControls"
        Title="MainWindow" Height="1000" Width="1024" MinWidth="1024">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <uc:NavListControl />
        </Grid>
        <Grid Grid.Row="1">
            <!--<ListView>
                <ListViewItem Content="01" />
                <ListViewItem Content="01" />
                <ListViewItem Content="01" />
            </ListView>-->
            <uc:NavAttributesControl />

        </Grid>
        <uc:SortUserControl Grid.Row="2" Margin="35,20" />
        <StackPanel Grid.Row="3" Margin="35,0">
         
        </StackPanel>
    </Grid>
</Window>
